#ifndef BIRD_H
#define BIRD_H

#include "Animal.h"
#include "animal.h

class Bird: public animal{
  private:
  float wingSpan;
  public:
  Bird();
  Bird(string name, int age, bool isHungry, float wingSpan):animal(string name,int age,bool isHungry),wingSpan(wingSpan);
};

//define class bird here 







#endif
